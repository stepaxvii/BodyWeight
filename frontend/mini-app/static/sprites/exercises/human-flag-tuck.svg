<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 56 56">
  <style>
    :root {
      --pixel-skin: #FFD5B8;
      --pixel-dark: #2D2D2D;
      --pixel-hair: #4A3728;
      --pixel-shirt: #4CAF50;
      --pixel-shorts: #1E88E5;
      --pixel-shoes: #424242;
      --pixel-bar: #795548;
      --pixel-accent: #FF5722;
    }
    .pulse { animation: pulse 1.6s ease-in-out infinite; }
    @keyframes pulse {
      0%, 100% { opacity: 0.9; }
      50% { opacity: 0.4; }
    }
  </style>

  <!-- Вертикальный шест -->
  <rect x="4" y="0" width="6" height="56" fill="var(--pixel-bar)"/>

  <!-- Руки держатся за шест -->
  <rect x="8" y="14" width="8" height="6" fill="var(--pixel-skin)"/>
  <rect x="8" y="28" width="8" height="6" fill="var(--pixel-skin)"/>

  <!-- Тело -->
  <rect x="14" y="18" width="16" height="14" fill="var(--pixel-shirt)"/>

  <!-- Голова -->
  <rect x="28" y="16" width="12" height="12" fill="var(--pixel-skin)"/>
  <rect x="34" y="20" width="2" height="2" fill="var(--pixel-dark)"/>
  <rect x="28" y="14" width="12" height="4" fill="var(--pixel-hair)"/>

  <!-- Шорты -->
  <rect x="14" y="30" width="10" height="8" fill="var(--pixel-shorts)"/>

  <!-- Ноги согнуты (tuck) -->
  <rect x="10" y="36" width="12" height="6" fill="var(--pixel-skin)"/>
  <rect x="20" y="36" width="6" height="10" fill="var(--pixel-skin)"/>
  <rect x="18" y="44" width="8" height="4" fill="var(--pixel-shoes)"/>

  <!-- Пульсирующий индикатор -->
  <circle class="pulse" cx="48" cy="50" r="2" fill="var(--pixel-accent)"/>
</svg>
